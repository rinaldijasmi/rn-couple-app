import React from 'react'
import ReactDOM from 'react-dom/client'
import WeddingPlanner from './WeddingPlanner.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WeddingPlanner />
  </React.StrictMode>,
)
